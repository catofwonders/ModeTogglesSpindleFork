{
  "name": "mode-toggles-spindle",
  "version": "1.0.0",
  "description": "Toggle non-diegetic modifier modes injected into LLM prompts — Spindle extension for Lumiverse",
  "author": "depFA",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bun run build:backend && bun run build:frontend",
    "build:backend": "bun build src/backend.ts --outfile dist/backend.js --target bun",
    "build:frontend": "bun build src/frontend.ts --outfile dist/frontend.js --target browser"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "latest"
  }
}
